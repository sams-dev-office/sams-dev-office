# Token Waste Watch — Wise Guy
One-liners: date | agent | pattern | fix
---
2026-07-09 | Wise Guy (self) | Fired afternoon deep-dive with NO morning-scan log to read — burned 2 web searches rediscovering today's category and candidate instead of inheriting them from a HANDOFF. | Guarantee the 09:00 fire actually runs and writes `log_YYYY-MM-DD_morning.md` with a HANDOFF block naming the deep-dive target, OR add a fallback rule: if morning log missing, pick category from date-mod-10 and log NEEDS_HELP before searching.

## 2026-07-11 (special fire)
- WASTE PATTERN: Vex fox orientation was verified with FOUR ad-hoc in-chat splat-render scripts (each ~60 lines piped through context). Should be ONE reusable `tools/view_glb.mjs --yaw N` in the game repo producing a PNG. → Action item routed to Neo with the S1 phase work. Estimated waste: ~15k tokens.
- GOOD PATTERN CONFIRMED: git+PAT from sandbox for all repo writes (zero file content through MCP); Higgsfield permanent source_game URL reuse on deploy retries (no re-upload).

2026-07-12 | Wise Guy (self) | Fired afternoon deep-dive AGAIN with no morning log — re-reviewed Superpowers that had already been decided (adopt/Neo-Website) on 2026-07-09. Same waste as 07-09 finding, unfixed. | Priority: implement the `last_handoff.md` symlink at `06_WiseGuy_ClaudeStack/last_handoff.md` pointing to the newest `_logs/log_*.md`. First Read at fire start = category + next-target in one call, zero searches to bootstrap.
