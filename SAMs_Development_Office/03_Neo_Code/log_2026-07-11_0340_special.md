# Neo — special fire 2026-07-11 ~03:40 UTC (CEO commands via Claudinho)

## Done (animal-kart repo, commit c8ff944, v23 DEPLOYED to https://serene-cello-891.higgsfield.gg/)
1. PHASE A GLOSS PASS: flat() factory -> MeshStandardMaterial kart recipe (roughness .3 / envMapIntensity 1.15); crowd .35, tires .45, item-box shell .12 + paw .25. Roads/sky/water/translucents untouched by design. 5 material sites, one choke point.
2. CONTROLLER SUPPORT (Xbox/DualSense/Switch Pro): analog stick steering w/ 0.15 deadzone (physics + NEW: wheel/driver visuals — pad steering previously didn't animate them), analog triggers at value>0.12, dual-rumble on hit/boost/pickup scaled by event gain (closes RESUME open item). PADB standard-mapping indices unchanged (positional = layout-safe on all three pad families).

## Verification
node tools/smoke.mjs exit 0; node tools/sync_test.mjs 18/18 PASSED; steer-sign + deadzone math hand-verified (stick-left == keyboard-left == +1). ~35 net lines. Sam test-drive on deploy = approval.

## ACK
- Casual stuck-lobby server bug: acknowledged, NOT touched this fire (per VP). Next fire priority if Sam confirms after test-drive. Note: ranked shares the room code path — treat as urgent.
- Track batch 2: seen, awaiting Sam approval — no tracks.js work done.

## HANDOFF next fire
1. If Sam approves batch 2: implement tracks one at a time, EACH with server.js TRACKS mirror + smoke + sync + minimap check. Feasibility spikes first: ice-grip client flag, pseudo-fork wide-split.
2. Stuck-lobby root fix: server.js lobby prune (ws close/heartbeat/ready-timeout) — needs Sam-test plan via VP (touches multiplayer).
3. Wise Guy suggested reusable tools/view_glb.mjs — fold in when convenient.
