# Neo — Log 2026-07-09 16:30 ET (afternoon coding fire)

## Continuity ritual
- Last log: none (this is Neo's first log).
- `inbox_from_vp.md`: does not exist. No routed Sam commands.
- `_meetings/inbox/`: 2 items — marketing ad brief (not for Neo), MoneyGuy→Clover asset prompt request (not for Neo).
- `00_VP_Claudinho/ceo_commands.md`: "(no commands yet)".
- `hamster_survivor/TODO.md`: single item — "waiting on repo initialization — Sam to provide repo path or approve `git init` here".
- `hamster_defense/TODO.md`: same — "waiting on repo initialization".

## Status: BLOCKED
Both game repos are un-initialized. Charter says never guess; TODO explicitly gates on Sam's approval before `git init`. No code work possible this fire.

Nothing implemented. Nothing to verify. Cannot fabricate a "done" — that violates the verification rule.

## What I need from Sam (unblock)
Decide, for each of `hamster_survivor/` and `hamster_defense/`:
- (A) Run `git init` in-place inside `03_Neo_Code/<game>/` and treat as sub-project inside the office repo, OR
- (B) Provide external GitHub repo URLs to clone into those folders (and my push credentials scope), OR
- (C) Point me at a starter template (e.g. Phaser 3 + Vite for Survivor, tower-defense scaffold for Defense) to bootstrap.

Once unblocked I can start on the top priority items Clovis/Clover flagged (Hamster Survivor arena loop is the highest-value target — MoneyGuy just filed a starter-kit asset brief for that IP, so code + assets can converge).

## SAM-TEST TONIGHT
No — nothing to test. No >100 line change, no multiplayer/save-state work.

## HANDOFF → tomorrow 10:30
Check `inbox_from_vp.md` first for Sam's unblock decision (A/B/C above). If unblocked, bootstrap the chosen game repo and commit an empty-scene "hello hamster" build as fire #1. If still blocked after 10:30, write `NEEDS_HELP.md` per charter (2 stuck fires in a row) and stop.

## Commit
Log-only commit this fire — no code changes.
