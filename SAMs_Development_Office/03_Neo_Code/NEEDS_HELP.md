# Neo — NEEDS_HELP

Date: 2026-07-09 (16:30 ET afternoon fire)

## Blocker
Both game repos (`hamster_survivor/` and `hamster_defense/`) are empty except for a TODO.md whose only item is:

> `[ ] (waiting on repo initialization — Sam to provide repo path or approve git init here)`

There is no `inbox_from_vp.md`, no `log_*.md` from prior fires, and no other tasked work. I cannot write, run, or verify game code because there is no engine scaffold, package.json, project file, or repo of any kind to build against.

## What I need from Sam (any one is enough to unblock)
1. Approve `git init` inside `03_Neo_Code/hamster_survivor/` and pick an engine (Godot 4, Phaser 3, LOVE2D, Unity, or raw HTML5 canvas). Once picked I can scaffold a minimal runnable "hamster moves on arena" build in the next fire and verify by running it.
2. OR point me at an existing repo (URL) to clone into that folder.
3. OR route a concrete first task through `inbox_from_vp.md` (e.g. "port this reference project", "prototype the wheel-spin mechanic in Phaser 3").

## Recommendation
Godot 4 for Hamster Survivor — 2D-first, free, exports to HTML5/desktop/mobile, matches Vampire-Survivors-clone tooling used by top itch.io competitors. Phaser 3 if you want browser-only for a free web demo funnel.

## What I did this fire
- Read README, TODOs, meetings inbox.
- Confirmed no code exists yet in either game folder.
- No commit — nothing to commit.

Handoff to tomorrow 10:30: unblock decision above, then scaffold + verify.
